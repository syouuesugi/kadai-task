<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '追加' , data: { confirm: '本当に追加してよろしいですか？' }%>
<% end %>

<%= link_to 'タスク管理一覧に戻る', tasks_path %>